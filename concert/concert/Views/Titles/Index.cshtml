@model IEnumerable<concert.Models.Title>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 style="color:blueviolet">คำนำหน้าชื่อ</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NameTi)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameTi)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.IDTitle }) |
                @Html.ActionLink("Details", "Details", new { id = item.IDTitle }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.IDTitle },
               new { onclick = "return confirm('คุณจะลบใช่มั้ย ?')" })
            </td>
        </tr>
    }

</table>
