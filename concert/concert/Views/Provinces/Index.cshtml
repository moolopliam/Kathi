@model IEnumerable<concert.Models.Province>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout - Copy.cshtml";
}

<h2 style="color:blueviolet">ชื่อจังหวัด</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NameProvince)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameProvince)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.IDProvince }) |
                @Html.ActionLink("Details", "Details", new { id = item.IDProvince }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.IDProvince },
               new { onclick = "return confirm('คุณจะลบใช่มั้ย ?')" })
            </td>
        </tr>
    }

</table>
